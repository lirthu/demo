class User:
        def __init__(self, user_id, surename, name, thirdname, id_role, role_name):
            self.user_id = user_id
            self.surename = surename
            self.name = name
            self.thirdname = thirdname
            self.id_role = id_role
            self.role_name = role_name
